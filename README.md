# mercaiindustries.store

The public Merc AI Industries site: one self-contained `index.html`. No build
step, no external requests (fonts are embedded), no source code. `CNAME` binds
the custom domain.
